# Omarchy OS configuration

## Keybinds
Use `keyd` utility to setup keybinds.
Copy config file to:
```bash
cp keyd.conf /etc/keyd/default.conf
```

## nvim
Copy nvim directory:
```bash
cp -R nvim ~/.config/nvim
```
